version: '2'

services:

  web:
    build: .
    user: root
    volumes:
      - .:/code/
    ports:
      - "8000:8000"
    command: python manage.py runserver 0.0.0.0:8000

  tests:
    extends: web
    command: py.test -s --cov=. --cov-report=html